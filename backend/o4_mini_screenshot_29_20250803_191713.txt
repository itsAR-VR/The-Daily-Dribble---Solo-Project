Context: I need to fill a form field with the following information:
Field type: text
Field name: Weight
Value to enter: 0.5

Please analyze the screenshot and tell me:
1. Can you see this field?
2. Is it a dropdown, text input, or autocomplete?
3. The exact selector to use
4. Any special steps needed (like clicking to open dropdown first)

Respond in JSON format:
{
    "field_visible": true/false,
    "field_type": "dropdown|text|autocomplete|checkbox|radio",
    "selector": "exact CSS or XPath",
    "steps": [
        {"action": "click|type|select", "selector": "...", "value": "..."}
    ],
    "warnings": ["any issues"]
}
Analysis: {
  "field_visible": true,
  "field_type": "text",
  "selector": "XPath=//label[contains(normalize-space(.), 'Item weight')]/following::input[1]",
  "steps": [
    {
      "action": "click",
      "selector": "XPath=//label[contains(normalize-space(.), 'Item weight')]/following::input[1]"
    },
    {
      "action": "type",
      "selector": "XPath=//label[contains(normalize-space(.), 'Item weight')]/following::input[1]",
      "value": "0.5"
    }
  ],
  "warnings": [
    "There is already a value (0.3) present\u2014ensure you clear it before typing 0.5"
  ],
  "page_analysis": "You are on the CellPex Network seller 'List Inventory' page under the Smartphones section. The 'List Inventory' form is fully visible. The 'PHONES & TABLETS' radio button is selected, Brand & Model is set to 'WTS'. Dropdowns for Condition, Memory, Color, Market Spec, SIM Lock, and Carrier are unselected. Price and Quantity fields are empty. Min. Order is set to 1. In the Shipping row, the first dropdown is 'Packing', followed by 'Item weight' input currently showing 0.3 and a unit dropdown set to 'kg', then an 'Incoterms' dropdown. 'Allow local pickup' is checked. No blocking pop-ups or errors are visible.",
  "current_state": "On the listing form, ready to edit the Item weight field.",
  "next_action": "type",
  "action_details": {
    "value": "0.5",
    "description": "Set the item weight input to 0.5"
  },
  "confidence": "high"
}
